#include "aluno.h"
#include <stdio.h>
#include <stdlib.h>

Aluno atribuir () {
    Aluno *a = (Aluno*) malloc (sizeof(Aluno));

    if (a == NULL) {
        return NULL;
    }

    printf ("Digite o nome do aluno: ")
    scanf




}
